# AXXAM

Plateforme immobilière & hébergement en Algérie.

## Structure

- `axxam/` — frontend Next.js
- `Backend/` — API Express / PostgreSQL

## Démarrage

```bash
# API
cd Backend && npm install && npm run dev

# Frontend
cd axxam && npm install && npm run dev
```
